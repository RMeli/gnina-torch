name: gninatorch
channels:
  - conda-forge
  - pytorch
dependencies:
  - pip

  - numpy

  - pytorch
  - ignite
  - torchvision

  - mlflow
  - scikit-learn>=1.0

  - tqdm

  - pip:
    - molgrid

  # devtools
  - pytest
  - pytest-cov
  - codecov
