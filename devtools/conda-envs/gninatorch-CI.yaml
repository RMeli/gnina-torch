name: gninatorch
channels:
  - pytorch
  - conda-forge
dependencies:
  - pip

  - numpy

  - pytorch
  - ignite
  - torchvision

  - mlflow<2.16
  - scikit-learn>=1.0

  - tqdm

  - pip:
    - molgrid

  # devtools
  - pytest
  - pytest-cov
  - codecov
