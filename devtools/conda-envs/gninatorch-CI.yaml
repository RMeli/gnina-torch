name: gninatorch
channels:
  - conda-forge
dependencies:
  - pip
  - numpy

  - mlflow<2.16
  - scikit-learn

  - tqdm

  - pip:
    - molgrid
    - pytorch
    - torchvision
    - pytorch-ignite

  # devtools
  - pytest
  - pytest-cov
  - codecov
